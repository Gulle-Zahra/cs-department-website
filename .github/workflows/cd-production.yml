name: CD — Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy-production:
    name: Deploy to Production (GitHub Pages)
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/production

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t cs-website:production .
          echo "✅ Docker image built successfully"

      - name: Test Docker container
        run: |
          docker run -d -p 8080:80 --name prod-test cs-website:production
          sleep 3
          curl -f http://localhost:8080 && echo "✅ Container is serving correctly"
          docker stop prod-test && docker rm prod-test

      - name: Prepare files for deployment
        run: |
          mkdir -p deploy-prod
          cp -r *.html css deploy-prod/

      - name: Deploy to GitHub Pages — production folder
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./deploy-prod
          destination_dir: production
          commit_message: "deploy: production from ${{ github.sha }}"

      - name: Print live URL
        run: echo "🚀 Live at https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/production"
